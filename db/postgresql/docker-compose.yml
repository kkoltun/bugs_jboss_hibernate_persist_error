version: '3.1'

services:
  db:
    image: 'postgres'
    restart: 'always'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: 'sa'
      POSTGRES_PASSWORD: 'password'
      POSTGRES_DB: 'simple_database'
